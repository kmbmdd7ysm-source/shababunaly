/**
 * External Spalding media was intentionally removed from the live catalogue.
 * Critical storefront media must ship with the application instead of relying
 * on a third-party CDN. Re-enable these records only after their approved
 * product photography has been stored under /public.
 */
export const spaldingOfficialProducts = [] as const;
