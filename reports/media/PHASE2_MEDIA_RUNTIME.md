# Phase 2 media runtime status

Current contract:

- 13 mapped hero placements.
- 13 distinct direct MP4 sources.
- Native `<video>` playback only.
- No YouTube/Vimeo iframe runtime.
- Static local poster fallback.
- No fake poster drift / speed-ramp logic.
- Active category/Custom/Stories still mappings are local project files.
- Direct hero video bytes are remote; they are not claimed as first-party hosted.

Authoritative machine-readable source audit: `reports/phase2/phase2-systems-audit.json`.
