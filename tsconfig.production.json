{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "skipLibCheck": false
  },
  "include": ["types/**/*.d.ts", "src/**/*.ts", "src/**/*.tsx", "api/**/*.ts"],
  "exclude": ["dist", "node_modules", "reports", "coverage", "coverage-project", "brand-quarantine", "vendor"]
}
